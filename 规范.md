# 第一至第三问统一符号说明

本文档是论文第一至第三问的总符号表。第一问的物理量定义为基础；第二、三问只通过日期、场景和预报版本索引扩展，不为同一物理量另造字母。第四问可在本文档末尾继续补充，但不应复用已有符号表示新含义。

## 1. 统一记号规则

1. 大写字母通常表示功率或模型层面的量，小写字母通常表示一个离散区间内的电量；功率乘以 $\Delta t$ 后得到区间电量。
2. 上标 $\mathrm{PV}$ 表示光伏，$\mathrm{act}$ 表示实际观测，$\mathrm{base}$ 表示基线，$(s)$ 表示场景，$(0)$ 表示0点原计划，$(\tau)$ 表示预报时刻 $\tau$ 确认的计划版本，$\mathrm{fin}$ 表示最终确认版本。
3. 波浪号 $\widetilde{\cdot}$ 表示附件给出的官方原始预报，帽号 $\widehat{\cdot}$ 表示模型预测或校准后预报。
4. 没有日期下标时，变量表示当前讨论日；全年回测或需要强调跨日时增加日期下标 $d$，如 $q_{d,t}$、$E_{d,t}$。
5. 向量、矩阵只在求解器矩阵形式中使用粗体；论文业务模型优先保留带物理意义的标量符号。
6. $(z)_+=\max\{z,0\}$ 表示正部函数；$\operatorname{clip}(z,0,1)$ 表示将 $z$ 截断到区间 $[0,1]$。

## 2. 集合、索引与时间

| 符号 | 含义 | 取值或单位 |
|---|---|---|
| $d$ | 日历日期或评价日索引 | 日 |
| $t$ | 日内10分钟时段索引 | $t=0,\ldots,T-1$ |
| $T$ | 一天包含的时段数 | $T=144$ |
| $\Delta t$ | 单个时段长度 | $1/6$ h |
| $s,s'$ | 场景编号；$s'$ 仅作第二个场景索引 | $s\in\mathcal S$ |
| $\mathcal S$ | 随机场景集合 | — |
| $j$ | 已完全兑现的历史日期索引 | 日 |
| $j_s$ | 场景 $s$ 抽取到的历史日期 | 日 |
| $\tau$ | 官方预报发布时间或购电计划调整时刻 | 时间戳 |
| $\delta$ | 官方预报的提前量列号 | $1,\ldots,24$ h |
| $\kappa$ | 被预测的目标时刻，$\kappa=\tau+\delta\mathrm{h}$ | 时间戳 |
| $\tau_{\mathrm{cut}}$ | 某次训练或拟合允许使用数据的截止时刻 | 时间戳 |
| $m$ | 月份或月度模型版本索引 | $1,\ldots,12$ |
| $g$ | 光伏偏差分组索引 | — |
| $K$ | 第三问一次滚动优化窗口的末端状态索引 | — |

说明：日期索引 $d$ 与放电变量 $d_t$ 字体相同，但前者只出现在下标位置或文字索引中，后者始终带时段下标，语义不会混淆。

## 3. 基础数据与物理参数

| 符号 | 含义 | 单位 |
|---|---|---|
| $L_{d,t}$ 或 $L_t$ | 第 $d$ 日第 $t$ 时段的小区负荷功率 | kW |
| $l_{d,t}=L_{d,t}\Delta t$ | 第 $d$ 日第 $t$ 时段的负荷电量 | kWh |
| $P_{d,t}^{\mathrm{PV}}$ 或 $P_t^{\mathrm{PV}}$ | 光伏发电功率；第一问中为题目给定预测值 | kW |
| $P_{d,t}^{\mathrm{PV,act}}$ | 实际光伏发电功率 | kW |
| $p_{d,t}=P_{d,t}^{\mathrm{PV}}\Delta t$ | 第 $d$ 日第 $t$ 时段的光伏电量 | kWh |
| $\pi_t$ | 第 $t$ 时段外网正常购电单价 | 元/kWh |
| $N_{d,t}=L_{d,t}-P_{d,t}^{\mathrm{PV}}$ | 净负荷功率 | kW |
| $n_{d,t}=N_{d,t}\Delta t$ | 净负荷电量 | kWh |
| $n_{d,t}^{\mathrm{act}}$ | 实际净负荷电量 | kWh |
| $E_{d,t}$ 或 $E_t$ | 第 $t$ 时段开始时储能设备的内部储电量 | kWh |
| $E_0$ | 整个仿真初始时刻的储电量 | kWh |
| $E_{144}$ | 一个完整自然日结束时的储电量 | kWh |
| $E_{d,0},E_{d,144}$ | 第 $d$ 日的日初、日末储电量 | kWh |
| $E_{d,0}^{\mathrm{act}}$ | 第 $d$ 日由真实回放传入优化器的日初储电量 | kWh |
| $E^{\min},E^{\max}$ | 储电量下、上限 | kWh |
| $\eta_c,\eta_d$ | 充电效率、放电效率 | 无量纲 |
| $P_c^{\max},P_d^{\max}$ | 微网侧最大充、放电功率 | kW |

本文参数取 $E^{\min}=1200$ kWh、$E^{\max}=10800$ kWh、$\eta_c=\eta_d=0.9$、$P_c^{\max}=P_d^{\max}=5000$ kW。144个区间对应145个状态，即 $E_0,E_1,\ldots,E_{144}$。

## 4. 购电、储能与结算决策变量

| 符号 | 含义 | 单位 |
|---|---|---|
| $q_{d,t}$ 或 $q_t$ | 正常购电量；第二问中为当日0点锁定的计划量 | kWh |
| $c_{d,t}$ 或 $c_t$ | 微网侧送入储能设备的充电量 | kWh |
| $d_{d,t}$ 或 $d_t$ | 储能设备送回微网侧的放电量 | kWh |
| $h_{d,t}$ 或 $h_t$ | 五倍价格的紧急购电量 | kWh |
| $w_{d,t}$ 或 $w_t$ | 无法利用的富余电量，即弃电量 | kWh |
| $a_t=q_t-n_t^{\mathrm{act}}$ | 实时反馈中的计划电量盈余；负值表示缺口 | kWh |
| $q_t^{(0)}$ | 第三问当日0点确定的原始计划购电量 | kWh |
| $q_t^{(\tau)}$ | 第三问在时刻 $\tau$ 更新后确认的计划购电量 | kWh |
| $q_t^{(\tau^-)}$ | 时刻 $\tau$ 调整发生前的上一版确认计划 | kWh |
| $q_t^{\mathrm{fin}}$ | 当天所有允许调整完成后的最终确认计划 | kWh |
| $u_t^{(\tau)}$ | 时刻 $\tau$ 相对上一版计划的增加量 | kWh |
| $v_t^{(\tau)}$ | 时刻 $\tau$ 相对上一版计划的减少量 | kWh |
| $J$ | 优化器用于生成购电方案的目标函数值 | 元 |
| $C$ 或 $C^{\mathrm{act}}$ | 按真实回放账本计算的费用 | 元 |
| $\Phi(E_K)$ | 第三问滚动窗口末端储电量的价值项，不计入实际费用 | 元 |
| $\nu$ | 末端单位储电量估值系数 | 元/kWh |

带场景上标的 $c_t^{(s)},d_t^{(s)},h_t^{(s)},w_t^{(s)},E_t^{(s)}$ 是优化模型中的场景补救变量；实际执行量不带场景上标。正常购电计划 $q_t$ 在同一次随机规划的全部场景中共享。

## 5. 第二问预测与场景符号

| 符号 | 含义 | 单位 |
|---|---|---|
| $N_{d,t}^{\mathrm{base}}$ | 净负荷周期基线 | kW |
| $\overline N_{d,t}^{\mathrm{wd}}$ | 过去同星期、同时段的净负荷均值 | kW |
| $\overline N_{d,t}^{(3)}$ | 最近3日同时段净负荷均值 | kW |
| $r_{d,t}=N_{d,t}^{\mathrm{act}}-N_{d,t}^{\mathrm{base}}$ | 基线残差，也是残差MLP训练标签 | kW |
| $x_{d,t}$ | 预测时刻可获得的特征向量 | — |
| $f_\theta(\cdot)$ | 参数为 $\theta$ 的残差MLP；$\theta$ 为网络参数集合 | — |
| $\alpha$ | MLP残差修正的收缩系数 | 无量纲 |
| $\widehat N_{d,t}$ | 0点发布的净负荷预测 | kW |
| $\varepsilon_{j,t}=N_{j,t}^{\mathrm{act}}-\widehat N_{j,t\mid j00{:}00}$ | 历史日期 $j$ 的完整样本外预测误差 | kW |
| $N_t^{(s)}=\widehat N_t+\varepsilon_t^{(j_s)}$ | 场景 $s$ 的净负荷功率 | kW |
| $\rho_s$ | 场景 $s$ 的概率，满足 $\sum_s\rho_s=1$ | 无量纲 |
| $\mathcal H_t^{(s)}$ | 场景 $s$ 在时段 $t$ 前已经揭示的信息历史或信息节点 | — |
| $\mathcal L_{\mathrm{Huber}}(z)$ | 训练残差MLP使用的Huber损失 | 无量纲 |

注意区分两类残差：$r_{d,t}$ 是相对于周期基线的训练标签；$\varepsilon_{j,t}$ 是当时完整模型发布后形成的样本外误差，只有后者进入随机场景库。

## 6. 第三问负荷与光伏预报符号

| 符号 | 含义 | 单位 |
|---|---|---|
| $L_{d,t}^{\mathrm{base}}$ | 负荷周期基线 | kW |
| $f_{\theta_L}(\cdot),\alpha_L$ | 负荷残差MLP及其收缩系数 | — |
| $\widehat L_{\kappa\mid\tau}$ | 时刻 $\tau$ 可用、目标为 $\kappa$ 的负荷预测 | kW |
| $\widetilde P_{\kappa\mid\tau}^{\mathrm{PV}}$ | 附件3在时刻 $\tau$ 发布的官方原始光伏预报 | kW |
| $e_{\kappa\mid\tau}^{\mathrm{PV}}$ | 官方光伏预报的历史误差 | kW |
| $b_g$ | 第 $g$ 个发布时间—提前量组的收缩平均偏差 | kW |
| $n_g$ | 第 $g$ 个偏差组内的历史有效样本数 | 个 |
| $\lambda_b$ | 平均偏差校正的验证选择系数 | 无量纲 |
| $f_{\theta_P}(\cdot)$ | 光伏官方预报误差的MLP校准函数 | kW |
| $x_{\kappa\mid\tau}$ | 时刻 $\tau$ 校准目标时刻 $\kappa$ 的光伏预报时可用的特征向量 | — |
| $D_d^{\mathrm{hist}}$ | 截至日期 $d$ 已积累的历史天数 | 日 |
| $\chi(D_d^{\mathrm{hist}})$ | 将历史天数映射到 $[0,1]$ 的时间爬坡函数 | 无量纲 |
| $\gamma_m$ | 月度验证选择的MLP可用比例 | 无量纲 |
| $\omega_d=\chi(D_d^{\mathrm{hist}})\gamma_m$ | 第 $d$ 日光伏MLP校准权重 | 无量纲 |
| $\widehat P_{\kappa\mid\tau}^{\mathrm{PV}}$ | 以官方预报为主体、经历史偏差与MLP校准后的光伏预报 | kW |
| $\widehat N_{\kappa\mid\tau}$ | 发布时刻 $\tau$ 对目标时刻 $\kappa$ 的净负荷预测 | kW |
| $\varepsilon_{\kappa\mid\tau}^{N}$ | 第三问净负荷的历史样本外预测误差 | kW |
| $N_{\kappa\mid\tau}^{(s)}$ | 第三问场景 $s$ 的净负荷功率 | kW |

条件竖线“$\mid$”表示信息版本，而不是除法。例如 $\widehat P_{\kappa\mid\tau}^{\mathrm{PV}}$ 表示“在 $\tau$ 时刻发布、针对 $\kappa$ 时刻”的光伏预测。同一目标时刻可以拥有0点、6点、12点、18点等不同版本，不能平均合并。

## 7. 第一问求解与证明中的辅助符号

以下符号只用于第一问的矩阵求解、经济性推导或最优性证书，不建议在第二、三问重新赋义：

| 符号 | 含义 |
|---|---|
| $Q=\sum_tq_t$ | 全天正常购电总量 |
| $\Lambda=\prod_k\eta_{k,c}\eta_{k,d}$ | 跨时段储能循环的总保留比例 |
| $\tau_t^c=c_t/P_c^{\max}$，$\tau_t^d=d_t/P_d^{\max}$ | 区间内充、放电持续时间 |
| $\boldsymbol x$ | 线性规划决策向量 |
| $\boldsymbol f$ | 线性目标系数向量 |
| $A,G,\boldsymbol b,\boldsymbol h$ | 等式/不等式约束矩阵及其右端项 |
| $\boldsymbol\ell,\boldsymbol u$ | 决策向量的下界、上界；只在矩阵形式中使用粗体 |
| $\boldsymbol y,\boldsymbol\mu$ | 对偶变量 |

## 8. 本次审计确定的禁用冲突

| 不再采用的写法 | 原因 | 统一写法 |
|---|---|---|
| 用 $b_t$ 表示放电量 | 与第三问偏差项 $b_g$ 易混淆，且第一问已采用 $d_t$ | 放电统一为 $d_t$ |
| 用 $E_{143}$ 表示日末状态 | 144个区间需要145个状态 | 日末为 $E_{144}$ |
| 将 $p_t$ 解释为负荷电量 | $l_t$ 已表示负荷电量 | $p_t$ 只表示光伏电量 |
| 用 $r$ 表示预报发布时间 | $r_{d,t}$ 已是基线残差 | 发布时间统一为 $\tau$ |
| 用 $T$ 表示某个目标时间戳 | $T=144$ 已表示日内时段数 | 目标时刻统一为 $\kappa$ |
| 用 $c$ 表示拟合截止时刻 | $c_t$ 已是充电量 | 截止时刻统一为 $\tau_{\mathrm{cut}}$ |
| 用 $w_d$ 表示校准权重 | $w_t$ 已是弃电量 | 校准权重统一为 $\omega_d$ |
| 用 $n_d$ 表示历史天数 | $n_{d,t}$ 已是净负荷电量 | 历史天数统一为 $D_d^{\mathrm{hist}}$ |
| 用 $\ell(\cdot)$ 表示Huber损失 | 与负荷电量 $l_t$ 视觉接近 | 损失统一为 $\mathcal L_{\mathrm{Huber}}$ |
| 混用 $\epsilon$ 与 $\varepsilon$ | 同一误差出现两种字形 | 样本外误差统一为 $\varepsilon$ |
| 混用 actual、final 与缩写 | 上标过长且格式不一 | 统一为 $\mathrm{act}$、$\mathrm{fin}$ |

## 9. 第四问预留

第四问新增符号时，应优先采用现有索引规则。例如电价预测可写成 $\widehat\pi_{\kappa\mid\tau}$，电价样本外误差可写成 $\varepsilon_{\kappa\mid\tau}^{\pi}$。最终符号应在第四问模型确定后补入，而不是提前占用已有字母。
