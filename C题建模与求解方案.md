# C题：微网与外部电网电力调控策略——建模与求解方案

> 文档用途：统一建模、代码实现、回测验证和论文写作口径。
>
> 当前状态：技术路线已确定，尚未在本文件中给出计算结果。网络参数、场景规模、终端处理及实际节省费用，需要通过严格时序实验确定。

## 1. 总体思路与四问关系

采用以下统一框架：

**周期基线＋收缩残差MLP → 历史样本外连续残差场景 → 非预见性随机MILP → 滚动执行 → 独立时序回测。**

第一问单独求确定性混合整数线性规划（MILP）的最优解。后面三问沿用同一电池模型、费用账本和校验器，只改变可用信息与购电调整权限。

| 问题 | 信息条件 | 常规购电权限 | 主方法 | 结果文件 |
|---|---|---|---|---|
| 第一问 | 当天负荷、电价和光伏预测作为确定输入 | 一次性安排全天 | 确定性MILP＋HiGHS | `result1.xlsx` |
| 第二问 | 使用决策时刻以前的历史观测 | 每天0点锁定当天计划 | 残差MLP＋连续场景＋随机MILP | `result2.xlsx` |
| 第三问 | 增加0、6、12、18点光伏预报 | 在规定更新时刻调整未执行计划 | 预报校正＋随机模型预测控制 | `result3.xlsx` |
| 第四问 | 增加波动电价 | 分别重复第二、三问的权限 | 联合场景＋随机滚动优化 | `result4-2.xlsx`、`result4-3.xlsx` |

MILP解决“满足约束时怎样行动”；MLP估计“未来净负荷”；场景描述“预测会怎样连续出错”；滚动控制负责“新信息到来后如何纠偏”。

### 1.1 主方案边界

- 主模型最小化期望购电费用，不预先加入主观风险偏好。
- CVaR只作为可选扩展，不属于基础交付。
- DP可用于第一问交叉验证，不决定最终提交电量的精度。
- 不增加强化学习、复杂集成或端到端可微优化作为主算法。
- 有限场景模型内的最优解，不等于真实未知未来中的绝对最优策略。

## 2. 题目事实、建模假设与待核对事项

### 2.1 题目给定的基础条件

- 储能额定容量为12000 kWh，运行储电量范围为1200～10800 kWh。
- 最大充放电功率为5000 kW，充放电效率均按90%处理。
- 2025年1月1日0点初始电量为6000 kWh。
- 调度输出粒度为10分钟，一天144个时段。
- 第一问要求日初日末电量相同；不能自动将这一要求扩大到第二至四问。
- 第二问紧急购电价格为相应交易电价的5倍。
- 第三问增加预报更新及购电调整费用。
- 第二至四问的要求结果覆盖2025年2月1日至12月31日，状态需要解释如何从1月初连续演化至2月初。

### 2.2 实施前必须固定的解释

| 事项 | 拟采用处理 | 影响 |
|---|---|---|
| 原始时刻标签 | 先核对其代表区间起点、终点还是瞬时采样；建立唯一时间映射 | 避免整体错位10分钟 |
| 功率转电量 | 将代表区间平均功率的数据乘以1/6小时；若为瞬时采样，声明所用积分近似 | 影响供需与费用 |
| 5000 kW计量侧 | 基础模型按微网侧充电输入、放电输出限值理解 | 决定效率放在哪一侧 |
| 富余电量 | 不计售电收入，允许明确记录的富余处置；若仅允许弃光，应限制弃光量 | 决定可行域 |
| 减购结算 | 暂按取消货款退回、另收50%违约费解释 | 决定第三问目标函数 |
| 重复调整 | 用逐次调整账本，明确每次相对哪份有效计划、按何价格结算 | 避免漏收或重复收费 |
| 波动电价信息 | 明确未来价格是否提前公布；未知时才能使用价格预测 | 防止数据穿越 |
| 区间内控制 | 区分区间开始决策、区间内反馈、区间结束结算 | 决定实时可执行性 |

上述存在解释空间的内容必须在论文“模型假设”中说明。费用和时间口径优先依据题面及适用的正式说明确定，不能为了获得更低费用任意选择。

## 3. 时间索引、变量与单位

令一天有 $T=144$ 个时段，$t=0,\ldots,T-1$，时段长度为：

$$
\Delta t=\frac{10}{60}=\frac16\quad\mathrm h.
$$

$d$ 表示日期，$k$ 表示预测或决策发布时间，$s$ 表示场景。为简洁，物理模型中暂省略日期和场景下标。

| 符号 | 含义 | 单位 |
|---|---|---|
| $L_t,P_t$ | 负荷功率、光伏功率 | kW |
| $l_t=L_t\Delta t$ | 区间负荷电量 | kWh |
| $p_t=P_t\Delta t$ | 区间光伏电量 | kWh |
| $N_t=L_t-P_t$ | 净负荷功率，可为负 | kW |
| $n_t=N_t\Delta t$ | 净负荷电量 | kWh |
| $q_t$ | 当前有效的常规购电约定量 | kWh |
| $h_t$ | 紧急购电量 | kWh |
| $c_t$ | 从微网输入电池的电量 | kWh |
| $d_t$ | 电池向微网输出的电量；不与日期下标混用 | kWh |
| $E_t$ | 时段开始时储电量 | kWh |
| $w_t$ | 富余处置电量 | kWh |
| $z_t$ | 充放电模式变量 | 0或1 |
| $\pi_t$ | 单位电价 | 元/kWh |
| $\rho_s$ | 场景概率，非负且总和为1 | 无量纲 |

代码中建议用 `discharge_kwh` 命名放电量，避免与日期变量混淆。所有优化变量统一用kWh，预测器可以使用kW，但进入优化器前只转换一次。

## 4. 统一物理约束

### 4.1 供需平衡

$$
q_t+h_t+p_t+d_t=l_t+c_t+w_t.
$$

等价的净负荷写法：

$$
q_t+h_t+d_t-c_t-w_t=n_t.
$$

解释：常规购电、紧急购电、光伏和电池放电，需要覆盖负荷、充电和富余处置。净负荷为负时表示光伏可能超过负荷，不能将其简单截断为零。

### 4.2 电池状态转移

$$
E_{t+1}=E_t+\eta_c c_t-\frac{d_t}{\eta_d},
\qquad \eta_c=\eta_d=0.9.
$$

例如输入100 kWh，电池增加90 kWh；向负荷输出90 kWh，电池减少100 kWh。因此往返效率为 $0.9^2=0.81$，不是90%。

### 4.3 容量、功率与互斥约束

$$
1200\le E_t\le10800,\qquad t=0,\ldots,T.
$$

令 $U=5000\Delta t=2500/3$ kWh，则：

$$
0\le c_t\le Uz_t,
\qquad
0\le d_t\le U(1-z_t),
\qquad z_t\in\{0,1\}.
$$

其中 $z_t=1$ 允许充电，$z_t=0$ 允许放电；两种模式都允许零动作。求解时使用精确的 $5000/6$，不提前四舍五入成833.33。

同时要求：

$$
q_t\ge0,\quad h_t\ge0,\quad w_t\ge0.
$$

若只允许弃光，则改为 $0\le w_t\le p_t$，且需要分别保留负荷、光伏场景，不能只凭净负荷给出弃光上界。不同处置规则不得在不同方法之间混用。

### 4.4 初值与跨日连续

$$
E_{2025\text{-}01\text{-}01,0}=6000,
\qquad
E_{d+1,0}=E_{d,T}.
$$

只有第一问要求：

$$
E_T=E_0.
$$

后续不能每天重置为6000，也不能无依据地强制每天首尾电量相同。

## 5. 费用账本与优化目标

### 5.1 第一、二问

第一问无紧急购电：

$$
C_1=\sum_t\pi_tq_t,\qquad h_t=0.
$$

第二问按计划量支付常规购电费：

$$
C_2=\sum_t\pi_tq_t+5\sum_t\pi_th_t.
$$

即使计划电量未被有效利用，也不能事后按实际利用量减少购电费。

### 5.2 第三问：一次调整的示例口径

以原计划 $q_t^0$ 调整为 $q_t$，在取消货款退回并收50%违约费的解释下，且原购电和调整采用同一结算电价：

$$
C_t^{\mathrm{regular}}
=\pi_tq_t^0
+1.5\pi_t(q_t-q_t^0)_+
-0.5\pi_t(q_t^0-q_t)_+,
$$

其中 $(x)_+=\max(x,0)$。

解释：增购1 kWh支付1.5倍价格；减购1 kWh退回1倍货款但支付0.5倍违约费，因此净减少0.5倍价格的费用。

人工核算：原计划10 kWh、电价1元/kWh，减少至6 kWh，费用为 $6+0.5\times4=8$ 元；增加至12 kWh，费用为 $10+1.5\times2=13$ 元。

### 5.3 多次调整账本

若确认每次相对上一份有效计划结算，定义：

$$
q_t^{(k)}=q_t^{(k-1)}+u_{k,t}-v_{k,t},
\quad u_{k,t},v_{k,t}\ge0.
$$

在同一正电价的上述口径下：

$$
C^{\mathrm{regular}}
=\sum_t\pi_tq_t^{(0)}
+\sum_k\sum_{t\ge k}
\left(1.5\pi_tu_{k,t}-0.5\pi_tv_{k,t}\right).
$$

不能把多次调整压缩为最后一次净变化。例如先增购后减购，即使最终计划回到原值，也可能已经产生费用。若规则按最初计划统一清算，应替换为对应账本，不能仍使用逐次公式。

第四问若购入价格、取消退款价格与调整时价格不同，按各笔交易分别记账，不能机械沿用上式的同一个 $\pi_t$。

### 5.4 优化费用与实际费用分开

滚动随机优化目标：

$$
\min\sum_s\rho_s
\left[C_{\mathrm{window}}^{(s)}
+\Phi(E_H^{(s)},\mathcal I_k)\right].
$$

$\Phi$ 是窗口之外的预计费用，不是真实支出。最终报告：

$$
C_{\mathrm{report}}
=C_{\mathrm{regular,actual}}+C_{\mathrm{emergency,actual}}.
$$

已经支付且无法改变的历史费用属于沉没成本，可在当前优化中略去，但必须保留在全年账本中。

## 6. 第一问：确定性MILP

给定附件1数据，最小化 $C_1$，满足第4节约束、禁止紧急购电以及首末电量相同。

使用HiGHS，记录可行解费用 $J_{\mathrm{inc}}$、求解器下界 $J_{\mathrm{bound}}$、最优性间隙和运行时间。不同接口对间隙定义可能不同，报告所用接口的原始指标，另外可用以下辅助指标：

$$
g=\frac{|J_{\mathrm{inc}}-J_{\mathrm{bound}}|}
{\max(1,|J_{\mathrm{inc}}|)}.
$$

只有求解状态与间隙支持时，才称为达到指定容差的全局最优解。

### 6.1 储电价值解释

令 $V_t(E)$ 是在固定剩余输入、固定终端要求下，从电量 $E$ 出发的最小后续费用。比较共同可行的 $E$ 和 $E+\delta$：

$$
v_t(E)\approx\frac{V_t(E)-V_t(E+\delta)}{\delta}.
$$

它衡量电池多存1 kWh能减少多少未来费用。MILP存在离散模式，优先使用有限差分重新求解，不把整数问题的对偶变量直接当作可靠边际价格。

### 6.2 DP可选校验

$$
V_t(E)=\min_{a\in\mathcal A_t(E)}
\left\{C_t(E,a)+V_{t+1}(f(E,a))\right\}.
$$

终止条件为 $V_T(E_0)=0$，其他不满足终端电量的状态为 $+\infty$。离散DP只给出网格内最优解，不能未经误差分析声称它等于连续最优解。

## 7. 第二问：周期基线＋收缩残差MLP

### 7.1 周期基线

对日期 $d$ 的目标时刻 $t$，取决策时已经可知的历史同期样本集合 $\mathcal A(d,t)$：

$$
N_{d,t}^{\mathrm{base}}
=\sum_{j\in\mathcal A(d,t)}\omega_jN_{j,t},
\qquad \omega_j\ge0,\quad\sum_j\omega_j=1.
$$

候选包括前一天、前一周及近期同类日期。数据不足时按预先规定的规则降级，不访问未来日期补齐。

### 7.2 残差网络

$$
y_{d,t}=N_{d,t}-N_{d,t}^{\mathrm{base}},
$$

$$
\widehat N_{d,t}
=N_{d,t}^{\mathrm{base}}+\alpha f_\theta(x_{d,t}),
\qquad0\le\alpha\le1.
$$

初始网络：输入层 → 64维隐藏层＋ReLU → 32维隐藏层＋ReLU → 单输出。层宽只是启动配置，不是已验证最优值。

输入包含历史日滞后、此前完整日曲线统计、已观测近期变化、目标时刻、星期和预测提前量。时刻采用周期编码，例如：

$$
x_{\sin}=\sin(2\pi t/144),
\qquad x_{\cos}=\cos(2\pi t/144).
$$

预测当天18点时，不能使用当天17点实际值作为0点计划的输入。对于不同发布时间，通过统一的信息过滤函数生成可用特征。

### 7.3 训练与收缩

可采用均方误差或Huber损失加权重衰减：

$$
\min_\theta\frac1m\sum_i
\ell(y_i,f_\theta(x_i))+\lambda\|\theta\|_2^2.
$$

标准化器只在训练段拟合；提前停止使用之后但仍早于当前评估日的验证段。$\alpha=0$退回基线，$\alpha=1$完整采用网络修正。$\alpha$和模型参数只通过历史验证选择。

网络、标准化器、基线权重和收缩系数作为同一版本保存。不能只更新标准化器而沿用未同步调整的网络。

## 8. 第三问：利用光伏预报修正净负荷

### 8.1 0点必须使用新增预报

记历史模型预测为 $\widehat N_{t\mid0}^{\mathrm{hist}}$，历史光伏参考为 $\widehat P_{t\mid0}^{\mathrm{hist}}$，题目预报为 $\widehat P_{t\mid0}^{\mathrm{forecast}}$：

$$
\widehat N_{t\mid0}^{\mathrm{Q3}}
=\widehat N_{t\mid0}^{\mathrm{hist}}
-\beta_{0,h}
\left(\widehat P_{t\mid0}^{\mathrm{forecast}}
-\widehat P_{t\mid0}^{\mathrm{hist}}\right).
$$

历史光伏参考使用与净负荷基线兼容的简单周期估计。此式是一种待验证的校正模型，不是无需验证的物理恒等式。

### 8.2 6、12、18点固定参考更新

$$
\widehat N_{t\mid k}
=\widehat N_{t\mid0}^{\mathrm{Q3}}
-\beta_{k,h}
\left(\widehat P_{t\mid k}^{\mathrm{forecast}}
-\widehat P_{t\mid0}^{\mathrm{forecast}}\right)
+r_{t\mid k}.
$$

- 两份预报必须对应同一目标时刻 $t$。
- 每次相对固定的0点参考，避免不同系数的连续累加产生路径漂移。
- $\beta_{k,h}$ 按提前量区间在过去数据上估计，使用正则化或收缩，样本不足时合并区间。
- $r_{t\mid k}$ 只学习已使用新光伏预报后仍未解释的误差，可由近期已观测误差做简单平滑修正。
- 在线修正不得读入尚未发生的目标值。

使用固定参考能明确比较预报更新的作用，但仍要通过样本外结果验证校正是否优于直接使用最新预报。

### 8.3 整点到十分钟

若预报代表整点瞬时功率，可使用分段线性或保形插值，并检查非负性和日出日落附近形状；若代表整小时平均功率，则保持区间能量。夜间约束依赖已知或由历史估计的日照规律，不能以全年未来实际值确定每日夜间边界。

## 9. 历史样本外连续残差场景

### 9.1 建立可审计的预测档案

每次预测保存发布时间、目标时间、模型版本、输入可用截止时刻和预测值。目标值实际出现后再写入：

$$
\varepsilon_{t\mid k}=N_t-\widehat N_{t\mid k}.
$$

必须使用历史样本外残差，而非训练拟合残差。若模型版本发生较大变化，可以在当时已有历史内做滚动重建，但每个历史折仍不能使用该折之后的数据。

### 9.2 条件匹配与连续抽样

$$
N_{t:t+H}^{(s)}
=\widehat N_{t:t+H}
+\widetilde\varepsilon_{t:t+H}^{(s)}.
$$

连续块按目标时刻对齐，优先匹配发布时间、提前量和近期误差尺度。季节等进一步分组只在样本支持时使用，不能将一年数据切成大量稀疏小组。

问题二与第三问不同发布时刻分别使用匹配的误差档案。不能在使用新预报后仍假设误差与旧预测完全一样。

本方案是“点预测＋完整残差场景”，不再从另一个预测分布采样后叠加同一份残差，以免重复计入不确定性。

### 9.3 累计缺口与场景质量

$$
A_{t,h}^{(s)}=\Delta t\sum_{j=t}^{t+h-1}N_j^{(s)}.
$$

检查 $h=6,18,36$，即1、3、6小时的累计净需求与累计预测误差。十分钟尖峰检验功率压力，连续偏差检验容量压力。

场景压缩后重新归一化概率，并检查尾部及连续缺口是否被压缩丢失。场景数量、块长度和压缩程度由历史验证中的费用稳定性与计算时间共同选择。

## 10. 非预见性随机MILP

### 10.1 决策信息集

记 $\mathcal I_k$ 为时刻 $k$ 已知的历史观测、已发布预报、当前SOC和有效购电计划。决策必须满足：

$$
a_k=\mu_k(\mathcal I_k).
$$

相同已观测信息的场景共享当前动作：

$$
\mathcal I_k^{(s)}=\mathcal I_k^{(s')}
\Longrightarrow a_k^{(s)}=a_k^{(s')}.
$$

仅仅给每个完整场景分别设置充放电变量，并不能实现这一要求。

### 10.2 场景树实现

将场景按已经揭示的历史逐层构造成共享节点，变量附着在节点上；不能按照未来轨迹来决定当前分组。在连续观测空间中，基于已观测特征的聚类是有限近似，需要说明节点划分和实际观测落入节点的规则。

每次执行后重新生成或更新场景树，只执行当前决策。当前购电约束仍按题目固定：第二问不能因为重新优化就改写0点计划。

### 10.3 节点上的物理约束

每个节点使用同一套电池方程、功率约束和供需平衡。父节点的末端电量传到子节点；若不同子节点代表区间内不同扰动，实现时必须区分扰动揭示前动作与揭示后允许的补救动作。

### 10.4 调整权限

第二问：当天 $q_t=q_t^{(0)}$ 固定，优化后续电池和紧急购电策略。

第三问：仅在0、6、12、18点更新允许修改的常规购电变量；已执行时段及其费用固定。保持原计划属于可行选择，调整费用进入目标后不再额外重复扣除。

## 11. 十分钟执行与独立仿真

必须区分以下时序：

1. 区间开始：只使用此前观测和已发布预报确定计划动作。
2. 区间内：若允许实时测量反馈，按明确的控制规则修正，并受功率和容量约束限制。
3. 区间结束：根据实际数据结算，更新SOC和误差档案。

附件若只给十分钟平均功率，不能把完整区间平均值当作区间开始的已知输入。

基础实现可先采用区间开始固定电池动作、区间结束计算紧急缺口与富余的离散近似：

$$
h_t=\left[n_t-q_t+c_t-d_t\right]_+,
\qquad
w_t=\left[q_t+d_t-c_t-n_t\right]_+.
$$

该式对应所声明的电量层面结算近似，不足以证明区间内所有瞬时功率均无缺口。采用区间内反馈控制时，需要独立的反馈规则，不能用整段未来数据优化区间开始动作。

滚动伪代码：

```text
初始化SOC、模型、历史档案和费用账本
按时间顺序遍历每个十分钟区间：
    读取此刻已经可用的信息
    若到达模型更新时刻：仅用已有历史更新模型与参数
    若到达预报发布时间：读取该次预报并更新预测
    构造条件残差场景和共享信息节点
    按本问权限固定或开放常规购电变量
    求解当前滚动MILP并检查解是否可行
    执行当前动作，应用明确的区间内补救规则
    用真实供需推进SOC，独立计算费用
    保存计划、动作、观测、费用和求解状态
```

求解超时时使用当前已验证的可行解；没有可行解时，采用预先定义的可行备用规则，例如当前电池闲置并由紧急购电补足第二至四问缺口。所有回退次数单独报告，不能静默填零。

## 12. 跨日与终端价值

有限窗口可能诱导电池在窗口结束前过度放电。使用前瞻窗口和终端费用估计：

$$
\Phi(E_H,\mathcal I_k).
$$

终端函数可从已有历史优化或策略评估中估计，采用分段线性表示。如果已经验证可用凸近似：

$$
\Phi(E)=\max_m(a_mE+b_m),
$$

可引入变量 $\phi$，添加 $\phi\ge a_mE+b_m$ 实现。不能未经证明就宣称原MILP价值函数必然凸；一般分段线性函数需要相应的分段建模。

终端近似只在过去数据上拟合，检验不同窗口长度及终端函数对真实费用、末端SOC的影响。每日跨日连续和窗口终端处理是两件事，前者不能代替后者。

1月从6000 kWh按可用信息运行；初始缺少历史时使用明确的先验和备用策略，并检查2月初SOC的敏感性。不能用完整1月数据反推1月初的最优控制。

全年结束时，各对照策略使用一致的末端条件或另外报告末端储电量差异，避免通过消耗更多库存制造不公平的费用优势。

## 13. 第四问：波动电价

若当天价格提前已知，将真实已公布价格输入优化器。若未来价格未知，使用历史周期价格基线和简单校正，形成：

$$
\pi_{t:t+H}^{(s)}
=\widehat\pi_{t:t+H}
+\widetilde\varepsilon_{\pi,t:t+H}^{(s)}.
$$

净负荷与价格的误差块成对抽取：

$$
\left(\widetilde\varepsilon_N^{(s)},
\widetilde\varepsilon_\pi^{(s)}\right).
$$

这保留历史上可能的关联，不能无依据地独立采样。预测范围和负电价处理依据实际数据及规则确定。

分别运行“波动价格＋第二问权限”和“波动价格＋第三问权限”。通过消融分析电池的价差收益和缺电备用价值，两者存在交互，不能直接将两个独立费用差相加当作总价值。

## 14. 参数选择与回测设计

### 14.1 时间嵌套

每个评估日使用更早的训练段拟合网络，更晚但仍早于评估日的验证段选择参数，当前和之后日期评估实际效果。训练、标准化、基线权重、$\alpha$、残差库、终端函数和场景参数都遵守这一边界。

使用调度费用选择配置：

$$
\xi_k^*=\arg\min_{\xi\in\Xi}
C_{\mathrm{validation},<k}(\xi),
$$

$\Xi$ 是预先限定的小规模候选集合。这个公式表示历史候选优选，不意味着找到所有网络或所有策略中的全局最优。

### 14.2 分阶段实验

1. 人工案例核对电池与费用账本。
2. 完成第一问及最优性验证。
3. 比较周期基线和少量残差MLP配置。
4. 固定预测器，比较确定性与随机调度。
5. 选择场景数量、块长度、窗口和末端处理。
6. 加入光伏更新，做时间更新消融。
7. 加入波动价格，完成两套权限版本。

### 14.3 主对照组

| 对照 | 目的 |
|---|---|
| 周期预测＋确定性调度 | 可靠基线 |
| 残差MLP＋确定性调度 | 网络修正贡献 |
| 残差MLP＋随机调度 | 不确定性处理贡献 |
| 加入预报的滚动调度 | 更新信息贡献 |
| 完全信息优化 | 相同规则下的理想费用下界 |

完全信息模型使用一致的物理、结算与末端条件，只放宽信息限制。它与实际策略的差距包含不可避免的信息损失，不能全部归因于算法不足。

预报实验同时做逐次加入和从完整策略中移除某次更新。每个策略独立演化SOC，不能逐日拼接不同策略的最优结果。

## 15. 指标与独立校验

预测指标：MAE、RMSE及1、3、6小时累计误差。不将MAPE作为净负荷主指标，因为净负荷可接近零或为负。

$$
\mathrm{MAE}=\frac1n\sum_i|N_i-\widehat N_i|,
\qquad
\mathrm{RMSE}=\sqrt{\frac1n\sum_i(N_i-\widehat N_i)^2}.
$$

实际节省率：

$$
R=\frac{C_{\mathrm{base}}-C_{\mathrm{method}}}{C_{\mathrm{base}}}\times100\%.
$$

当基准费用为零或负值时，改报告绝对费用差，避免百分比失真。

报告总费用及分项、紧急购电量、调整电量、每日费用分布、约束残差、求解时间分位数、超时及回退次数。按月份报告收益，必要时对连续日块做重采样评估差异稳定性，不能将所有时段视为独立样本。

独立校验器重新检查：

$$
|q_t+h_t+p_t+d_t-l_t-c_t-w_t|\le\epsilon_E,
$$

$$
|E_{t+1}-E_t-0.9c_t+d_t/0.9|\le\epsilon_E.
$$

并检查容量、功率、互斥、首末与跨日条件、信息时间戳、费用逐笔重算、模板日期和缺失值。数值容差与变量单位和求解器精度一致，记录实测最大残差，不只显示“通过”。

## 16. 代码结构与实施顺序

| 模块 | 内容 |
|---|---|
| 数据与信息集 | 原始文件读取、时间映射、单位转换、可用数据过滤 |
| 电池 | 状态方程、功率与容量约束、执行规则 |
| 结算 | 原计划、逐次调整、紧急购电账本 |
| 预测 | 周期基线、残差MLP、收缩与光伏预报修正 |
| 场景 | 样本外预测档案、条件分块抽样、场景树 |
| 优化 | 确定性MILP、随机MILP、求解状态与回退 |
| 仿真 | 按时序执行、状态更新、独立费用重算 |
| 验证与输出 | 约束检查、对照实验、图表与结果模板 |

实施里程碑：

1. 固定时间与结算解释，建立小例子测试。
2. 求出第一问参考解，完成结果校验。
3. 用周期基线跑通完整时序仿真。
4. 加入收缩残差MLP，验证其增益。
5. 加入连续场景与非预见性决策，控制计算规模。
6. 完成第三问预报更新及费用分析。
7. 完成第四问两套结果及统一实验。
8. 独立校验后填写五份Excel模板，生成论文图表。

## 17. 论文表达与预期成果

论文主要贡献围绕两个可验证问题展开：

**持续预测误差下怎样分配储能容量。**既保留电量应对后续缺口，也保留空余容量吸收光伏，利用场景和边际价值解释其变化。

**新增光伏预报何时有经济价值。**综合调整费用、储能状态与后续缺口风险，量化各次更新的实际贡献和交互效果。

推荐图表包括：第一问购电与充放电曲线、SOC轨迹；预测与连续误差示例；不同策略费用分解；更新时刻与SOC对应的调整行为；月份收益；场景规模与求解时间敏感性。

这些属于待实验支持的模型特点，不预先宣称是全新理论，也不预先宣称能降低某个百分比费用。最终结论必须来自时序回测和独立校验。

## 18. 资料与来源

- 赛题原文：[C题.pdf](C题/C题.pdf)。约束、费用及提交要求以原文和适用正式说明为准。
- 数据与结果模板：`C题/附件/附件1.xlsx`至`附件4.xlsx`，以及`C题/附件/附件5/`。
- HiGHS与SciPy MILP接口说明：<https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.milp.html>。
- 储能运行与凸优化方法参考：<https://www.web.stanford.edu/~boyd/papers/storage_opt.html>。

本方案包含明确的建模选择与待核对假设。外部方法参考不能替代对本题结算规则、互斥条件和信息时序的逐项验证。
